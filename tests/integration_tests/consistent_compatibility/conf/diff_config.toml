# diff Configuration.

check-thread-count = 4

export-fix-sql = true

check-struct-only = false

[task]
    output-dir = "/tmp/tidb_cdc_test/consistent_compatibility/sync_diff/output"

    source-instances = ["tidb"]

    target-instance = "mysql"

    target-check-tables = ["consistent_compatibility.usertable*","consistent_compatibility.check*","consistent_compatibility.table*"]

[data-sources]
[data-sources.tidb]
    host = "127.0.0.1"
    port = 4000
    user = "root"
    password = ""
    snapshot = "<placeholder>"

[data-sources.mysql]
    host = "127.0.0.1"
    port = 3306
    user = "root"
    password = ""
