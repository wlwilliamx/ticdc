# diff Configuration.

check-thread-count = 4

export-fix-sql = true

check-struct-only = false

[task]
    output-dir = "/tmp/tidb_cdc_test/in_flight_syncpoint_during_scheduling/sync_diff/output"

    source-instances = ["tidb0"]

    target-instance = "mysql1"

    target-check-tables = ["sp_move.*"]

[data-sources]
[data-sources.tidb0]
    host = "127.0.0.1"
    port = 4000
    user = "root"
    password = ""

